package timjan.syntax;

/**
 * See also {@link Visibility} and {@link AnnotationStatement}.
 *
 * @author fredrik
 */
public enum Modifiers implements Modifier {
	STATIC, ABSTRACT, NATIVE, SYNCHRONIZED, TRANSIENT, VOLATILE, STRICTFP;
}